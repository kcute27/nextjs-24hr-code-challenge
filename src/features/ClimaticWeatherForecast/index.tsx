import React from "react";

const index = () => {
  return (
    <div>
      {/* This is simply a mock feature directory that demonstrates how this app
      should scale. */}
    </div>
  );
};

export default index;
